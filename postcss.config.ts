// PostCSS config for Tailwind CSS v4 using the @tailwindcss/postcss plugin.
// TypeScript is supported via postcss-load-config v4+.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
