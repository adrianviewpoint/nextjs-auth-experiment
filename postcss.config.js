// PostCSS config for Tailwind CSS v4 using the @tailwindcss/postcss plugin.
// Keep a JS-based config so Next.js/Turbopack can load it during build/dev.

module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
